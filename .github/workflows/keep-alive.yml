name: Keep Railway Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # кожні 10 хвилин
  workflow_dispatch:        # можна запустити вручну

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: |
          curl -I https://alexlight-production.up.railway.app || echo "Ping failed"
