name: Keep Railway Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # кожні 10 хвилин
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl -fsS https://alexlight.onrender.com || echo "Ping failed"
